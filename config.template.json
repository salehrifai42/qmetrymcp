{
  "_instructions": "Only 'project' is required. All other fields are optional. API key + region are NOT read from this file — set them via the QTM4J_API_KEY / QTM4J_REGION env vars (in .env or passed on the MCP launcher with -e). This is the single source of truth.",

  "project": {
    "_note": "Your numeric Jira project ID. Find in any project URL: ?projectId=<numeric-id>&projectKey=<KEY>",
    "projectId": 0
  },

  "xlsxImport": {
    "_note": "Defaults for scripts/import-xlsx-to-qmetry.py. CLI flags override these. componentIds = default components attached to every imported case; leave [] for none.",
    "parentFolderId": 0,
    "statusId": 0,
    "apiTestFieldId": "qcf_0",
    "apiTestOptionYes": "0",
    "apiTestOptionNo": "0",
    "componentIds": []
  },

  "executionResultIds": {
    "_note": "Required only for update_test_execution and bulk_update_test_executions. Verify IDs match your QMetry instance.",
    "Unexecuted": 1,
    "Pass": 2,
    "Fail": 3,
    "In Progress": 4,
    "Blocked": 5,
    "Not Applicable": 6
  },

  "optionalFields": {
    "_note": "Fill in only the fields you actually use.",
    "assigneeAccountId": "YOUR_JIRA_ACCOUNT_ID",
    "testCaseFolderId": 0,
    "testCycleFolderId": 0,
    "testPlanFolderId": 0,
    "environmentId": 0,
    "buildId": 0,
    "customFieldId": "qcf_1"
  }
}
